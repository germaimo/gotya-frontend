import React from "react";

const MidiTracks = ({midiTracks}) => {
  return (
    <div>
      <h2>Pistas MIDI:</h2>
      <MidiTracks midiTracks={midiTracks} />
    </div>
  );
};

export default MidiTracks;
